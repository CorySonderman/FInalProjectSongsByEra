{ // This reference the MySql database so that we can access the data.  It is very important
  // that you add appsettings.json to you gitignore file so that no one can access your password
  "ConnectionStrings": {
    "final_project_songsbyera": "Server=localhost;Database=final_project_songsbyera;uid=root;Pwd=password;Port=3306;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*"
}